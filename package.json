{
  "name": "temnolimao-site",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "minify": "npx terser tnl-app.js --compress passes=2 --mangle --output tnl-app.min.js && npx postcss tnl-style.css -o tnl-style.min.css"
  },
  "devDependencies": {
    "cssnano": "^8.0.1",
    "postcss": "^8.5.15",
    "postcss-cli": "^11.0.1",
    "terser": "^5.47.1"
  }
}
